import 'package:flutter/material.dart';

/// Manage the all dark theme colors.
class DarkColors {
  const DarkColors._();

  static const Color primary = Color(0xFF0F044C);
  static const Color primaryVariant = Color(0xFF141E61);
  static const Color secondary = Color(0xFF787A91);
  static const Color secondaryVariant = Color(0xFFEEEEEE);
}

/// Manage the all light theme colors.
class LightColors {
  const LightColors._();

  static const Color primary = Color(0xFFF0ECE3);
  static const Color primaryVariant = Color(0xFFDFD3C3);
  static const Color secondary = Color(0xFFC7B198);
  static const Color secondaryVariant = Color(0xFFA68DAD);
}
